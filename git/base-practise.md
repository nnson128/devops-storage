### base practise ###
1. push project from lab server ___
```
# Create group
1. create group | group includes subgroups || projects || microservices || Frontend || Backend
    - private
    - role: devops engineer
    - just me
    ``` create group
```
```
2. new projects 
    - project name: shoeshop
    - close "Initialize repository with a README"
    ``` create project
```
```
# lab server
1. git configure
    - git clone http://git.nnson128.tech/shoeshop/shoeshop.git
    - cp -rf /projects/shoeshop/* /data/shoeshop
    - git push -f origin
```
