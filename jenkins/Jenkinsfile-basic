pipeline {
    agent {
        label 'lab-server'
    }
    stages {
        stage('in4') {
            steps {
                sh(script: """
                    whoami 
                    pwd
                    ls -la
                """, label: 'build with maven')
            }
        }
    }
}
